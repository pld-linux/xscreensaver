--- xscreensaver-4.02/configure.in.wiget	Thu May  9 13:35:56 2002
+++ xscreensaver-4.02/configure.in	Thu May  9 13:36:41 2002
@@ -2029,7 +2029,7 @@
     xml_path="$foo:$xml_path"
   fi
 
-  AC_PATH_PROGS(xml_config, xml2-config xml-config,, $xml_path)
+  AC_PATH_PROGS(xml_config, xml-config xml2-config,, $xml_path)
 
   # If we found the xml-config program, run it to get flags.
   #
